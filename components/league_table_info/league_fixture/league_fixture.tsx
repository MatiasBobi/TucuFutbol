import { View } from 'react-native';
import { GameFilter } from '../../../types/league_full_info';
export default function LeagueFixture({
  league_fixture,
}: {
  league_fixture: GameFilter[];
}) {
  return (
    <View>
      <View>{/* Titulo de la tabla */}</View>
    </View>
  );
}
